需要**替换业务对象、调整信号类型、并重绘算法图表内容**。

以下是具体的改造方案：

### 一、 顶部导航与操作栏改造
*   **系统名称（左上角）**：将 `BearingDiag` 改为 `JetFan Monitor`（射流风机监测系统）。
*   **左侧菜单**：
    *   `深度诊断` -> 改为 `单机诊断监测`（当前页面）。
    *   `轴承信息` -> 改为 `风机台账`（管理风机编号、安装位置、额定转速2000r/min等）。
    *   保留 `实时监控看板`、`系统设置参数`。
*   **顶部操作区**：
    *   `轴承型号: FAG-6205` -> 改为 `风机编号: [下拉选择，如：隧道A-01号风机]`。
    *   `1500 RPM` -> 改为 `额定转速: 2000 RPM`（根据文档，这是固定转速，可作为只读显示，旁边增加一个 `运行状态: 运行中/停机` 的指示灯）。
    *   `执行AI诊断` -> 改为 `执行基线标定`（文档强调需要建立电流2小时基线和倾角24小时基线，这个按钮很重要）。
    *   `导出诊断报告` -> 去除。

### 二、 上方信号展示区改造（原“原始时域波形 vs 角域重采样波形”）
这部分用于展示最原始的传感器数据，让专家可以看到底层信号。
*   **标题**：改为 `多源传感器实时信号监测`。
*   **上图（原时域波形）**：改为 **`振动加速度时域/频谱图`**。
    *   用于展示叶片不平衡和螺栓松动的原始振动情况。X轴可以是频率(Hz)，重点展示 0~200Hz 范围（涵盖转频33.33Hz及叶片通过频率）。
*   **下图（原角域波形）**：改为 **`电流与倾角混合趋势图`**（或分列左右）。
    *   左Y轴显示电流(A)，右Y轴显示倾角(°)。用于直观对比负载变化和姿态漂移。

### 三、 下方算法卡片区改造（核心部分）
原有的四个图表区域（2x2布局）非常适合作为三个主要算法的展示区，我们可以这样分配：

#### 1. 左上图：螺栓松动算法监测（倾角主导）
*   **标题**：`螺栓松动监测 (倾角漂移分析)`
*   **图表内容**：
    *   **X轴**：时间（天/小时），**Y轴**：倾角漂移量 $\Delta\theta$ (°)。
    *   **曲线**：绘制经过低通滤波和滑动窗口处理后的 $\Delta\theta$ 趋势线。
    *   **阈值线**：画出两条水平虚线，分别是预警阈值 $\Delta\theta_1$ (如0.1°) 和报警阈值 $\Delta\theta_2$ (如0.3°)。
    *   **关键指标显示**：在图表旁或内部显示 `当前漂移量: 0.05°`，`漂移速率 dθ/dt: +0.002°/天`（根据文档，速率>0且超限才报警）。

#### 2. 右上图：叶片不平衡监测分析（振动主导）
*   **标题**：`叶片不平衡监测 (振动频谱特征)`
*   **图表内容**：
    *   **X轴**：频率 (Hz) 或 阶次 (Order)，**Y轴**：振动幅值。
    *   **曲线**：FFT频谱图。
    *   **特征标记**：用垂直线高亮标出 $1\times f_r$ (33.33 Hz) 和 $2\times f_r$ (66.67 Hz)。
    *   **关键指标显示**：显示 `1×fr幅值 A1`（及EWMA平滑后的趋势），`谐波比 R21 = A2/A1`（文档要求<0.3），`相位稳定性 σφ`（文档要求<15°）。

#### 3. 左下图：风口堵塞预警分析（电流主导）
*   **标题**：`风口堵塞预警 (电流RMS偏差)`
*   **图表内容**：
    *   **X轴**：时间，**Y轴**：归一化偏差 $\Delta I$。
    *   **曲线**：电流RMS相对于基线 $\mu_I$ 的偏差曲线。
    *   **阈值线**：画出 $\pm 3\sigma_I$ 的阈值带。
    *   **持续性进度条**：文档提到需要持续60s才报警。可以在图表下方加一个进度条，显示 `超限持续时间: 45s / 60s`，直观展示抗干扰逻辑。

#### 4. 右下图（原空白图）：辅助特征与环境补偿
*   **标题**：`辅助验证与环境补偿`
*   **图表内容**：
    *   由于文档提到了温度对倾角的影响，以及振动峭度对螺栓松动的辅助判断。
    *   可以绘制 **`环境温度 vs 倾角零偏`** 曲线，展示温度补偿算法的工作状态。
    *   或者绘制 **`振动峭度/峰值因子`** 趋势，作为螺栓松动出现非线性间隙的辅助证据。

### 四、 交互逻辑补充
*   **停机自适应**：根据文档，倾角监测是7x24小时的，而振动和电流仅在运转时有效。当顶部状态显示“停机”时，右上图（叶片不平衡）和左下图（风口堵塞）应置灰或显示“风机停机，监测暂停”，而左上图（螺栓松动）保持活跃。
*   **基线学习弹窗**：点击顶部的“执行基线标定”后，应弹出进度条，提示“正在采集电流基线（剩余 1小时50分）...”或“正在采集倾角基线（剩余 23小时...）”。

现有的界面框架完全可以复用，且能完美契合您文档中的算法逻辑。按照这个映射关系修改图表的数据源和坐标轴标签。