<UserControl x:Class="BearingFaultDiagnosis.Views.Pages.DashboardView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                     xmlns:scottplot="http://schemas.scottplot.net/2008/xaml/presentation"

      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:BearingFaultDiagnosis.Views.Pages"
        xmlns:collections="clr-namespace:BearingFaultDiagnosis.Models"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"

      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      >
    <UserControl.Resources>
        <Style x:Key="CardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="16"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#e2e8f0" BlurRadius="8" ShadowDepth="1" Opacity="0.4"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogTextStyle" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Margin" Value="0,2"/>
        </Style>

        <Style x:Key="ToolButtonStyle" TargetType="Control">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Medium"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#e2e8f0" BlurRadius="4" ShadowDepth="1" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Control" BasedOn="{StaticResource ToolButtonStyle}">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="White"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <Border Grid.Row="0" Background="Transparent" Margin="0,0,0,16">
          
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="7*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Sensor Cards -->
                <UniformGrid Grid.Row="0" Columns="5" Height="90" Margin="10,0,0,16">
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="设备温度" Foreground="#94A3B8" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="{Binding CurrentSensorData.Temperature, StringFormat={}{0:F1}, FallbackValue=25.4}" FontSize="28" FontWeight="SemiBold" Foreground="#0F172A"/>
                                <TextBlock Text="℃" FontSize="13" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="运行电流" Foreground="#94A3B8" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="{Binding CurrentSensorData.Current, StringFormat={}{0:F2}, FallbackValue=5.20}" FontSize="28" FontWeight="SemiBold" Foreground="#0F172A"/>
                                <TextBlock Text="A" FontSize="13" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="环境湿度" Foreground="#94A3B8" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="{Binding CurrentSensorData.Humidity, StringFormat={}{0:F1}, FallbackValue=45.0}" FontSize="28" FontWeight="SemiBold" Foreground="#0F172A"/>
                                <TextBlock Text="%" FontSize="13" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}" Margin="0,0,12,0">
                        <StackPanel>
                            <TextBlock Text="横滚角" Foreground="#94A3B8" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="{Binding CurrentSensorData.Roll, StringFormat={}{0:F2}, FallbackValue=1.05}" FontSize="28" FontWeight="SemiBold" Foreground="#0F172A"/>
                                <TextBlock Text="°" FontSize="13" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>

                    <Border Style="{StaticResource CardStyle}">
                        <StackPanel>
                            <TextBlock Text="俯仰角" Foreground="#94A3B8" FontSize="12"/>
                            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                                <TextBlock Text="{Binding CurrentSensorData.Pitch, StringFormat={}{0:F2}, FallbackValue=-0.32}" FontSize="28" FontWeight="SemiBold" Foreground="#0F172A"/>
                                <TextBlock Text="°" FontSize="13" Foreground="#94A3B8" VerticalAlignment="Bottom" Margin="2,0,0,3"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </UniformGrid>

                <!-- Chart -->
                <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="0" Margin="10,0,0,0">
                    <Grid>
                        <scottplot:WpfPlot Name="WpfPlot1" Grid.Row="1" Margin="10"/>
                    </Grid>
                </Border>
            </Grid>

            <!-- Log Panel -->
            <Border Grid.Column="1" Style="{StaticResource CardStyle}">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Text="系统实时日志" FontSize="15" FontWeight="SemiBold" Foreground="#1E293B" Margin="0,0,0,8"/>
                    <Rectangle Grid.Row="1" Height="1" Fill="#F1F5F9" Margin="0,0,0,8"/>

                    <ListBox Grid.Row="2" ItemsSource="{Binding DisplayList}"
                            x:Name="dataListLog"
                            BorderThickness="0" Background="Transparent"
                            VirtualizingStackPanel.IsVirtualizing="True"
                            VirtualizingStackPanel.VirtualizationMode="Recycling"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled">

                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Focusable" Value="False"/>
                                <Setter Property="IsHitTestVisible" Value="False"/>
                                <Setter Property="Padding" Value="0,4"/>
                                <Setter Property="BorderThickness" Value="0,0,0,1"/>
                                <Setter Property="BorderBrush" Value="#F8FAFC"/>
                            </Style>
                        </ListBox.ItemContainerStyle>

                        <ListBox.Resources>
                            <DataTemplate DataType="{x:Type sys:String}">
                                <TextBlock Style="{StaticResource LogTextStyle}">
                                   <Run Text="[系统] " Foreground="#94A3B8"/>
                                   <Run Text="{Binding .}" Foreground="#475569"/>
                                </TextBlock>
                            </DataTemplate>

                            <DataTemplate DataType="{x:Type collections:SensorDisplayItem}">
                                <TextBlock Style="{StaticResource LogTextStyle}">
                                   <Run Text="[" Foreground="#94A3B8"/>
                                   <Run Text="{Binding Time}" Foreground="#3B82F6"/>
                                   <Run Text="]" Foreground="#94A3B8"/>
                                   <Run Text=" 数据帧 |" Foreground="#475569"/>
                                   <Run Text=" 温度:" Foreground="#64748B"/>
                                   <Run Text="{Binding Temperature, StringFormat={}{0:F1}}" Foreground="#0F172A"/>
                                   <Run Text=" 湿度:" Foreground="#64748B"/>
                                   <Run Text="{Binding Humidity, StringFormat={}{0:F1}}" Foreground="#0F172A"/>
                                   <Run Text=" CntQ:" Foreground="#64748B"/>
                                   <Run Text="{Binding CountQ}" Foreground="#0F172A"/>
                                </TextBlock>
                            </DataTemplate>
                        </ListBox.Resources>
                    </ListBox>
                </Grid>
            </Border>

        </Grid>
    </Grid>
</UserControl>